# NovaTools
# Replace REPLACE-ME with your domain in this file, and set `origin` in
# nt-config.js to the same value.

User-agent: *
Allow: /

# Nothing here is private, but a filtered view of the directory is the same
# page with a different query string, and there is no value in indexing 11
# copies of it.
Disallow: /*?adpreview=
Disallow: /*&adpreview=

# While the site is new, point this at sitemap-launch.xml — the pages, the
# hand-written tools and the 87 conversions people search for. Switch to
# sitemap.xml, which adds the other 1,545 generated conversion pairs, once the
# site is approved. See README.md.
Sitemap: https://REPLACE-ME/sitemap-launch.xml
